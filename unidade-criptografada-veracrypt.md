# Usando o Veracrypt para criar unidades criptografadas

O *VeraCrypt*, disponível no para `Windows`, `MAC` e `Linux` no [site oficial](https://veracrypt.io/), serve para criptografar unidades completas e/ou para montar unidades virtuais com arquivos.

